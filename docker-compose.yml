services:
  missing-calls:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - missing-calls-data:/app/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  missing-calls-data:
